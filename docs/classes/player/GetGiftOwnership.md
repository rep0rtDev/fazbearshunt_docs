> <span class="fh-badge shared">shared</span> @bool@ @Player@:GetGiftOwnership()

# Описание

Возвращает статус того, есть ли у игрока несобранный подарок.

# Возвращает

> `1` @bool@

- Имеет ли игрок несобранный подарок?