> <span class="fh-badge shared">shared</span> @bool@ @Player@:GetOnlyAudibleByTeammates()

# Описание

Возвращает текущий статус слышимости игрока только союзниками.

# Возвращает

> `1` @bool@

- Игрока могут слышать только его союзники?